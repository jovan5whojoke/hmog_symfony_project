{% extends 'base.html.twig' %}

{% block body %}

    <form action="" method="post" novalidate>
        {{ form_errors(form) }}

        {{ form_rest(form) }}

        <button type="submit">Submit</button>
    </form>
{% endblock %}

